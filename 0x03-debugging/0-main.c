#include "main.h"

/**
* main - tests function that prints if integer is positive or negative
* positive_or_negative - tests for negative or positive number
* @i: the test integer
* Return: 0
*/
void positive_or_negative(int i);
int main(void)
{
	positive_or_negative(0);
	return (0);
}
